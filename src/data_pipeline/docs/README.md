# Moved

Pipeline documentation now lives at **`docs/data_pipeline/`** (repo root).
The design/spec corpus is at `docs/data_pipeline/specs/`, and the SeaHub
refactor at `docs/refactors/seahub/`.

See `docs/README.md` for the full layout and the rule for placing new docs.
