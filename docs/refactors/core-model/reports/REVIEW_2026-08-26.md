# R1 morning review — overnight threads

**Review date:** 2026-08-27 PDT. **Dispatch base:** `3ecc4818994de5c2ab9db1cafa85f1af60c64044`.
This review compares branch contents, worktree state, tests, source files, and data artifacts; thread
self-reports are not treated as evidence. The dispatch base and worktree/branch map were measured with
`git worktree list --porcelain`, `git branch --all`, and `git rev-parse HEAD` on 2026-08-27.

## Bottom line

Only T2a produced a commit, and it is not acceptance-complete. T1, S2, and A1 left useful work only as
uncommitted or untracked files; T3 produced nothing; S1 produced nothing. T3 completed **0 of 8**
acceptance steps. The required legal-positive preflight and small-cohort split-ratio failure never ran.
T1 found **23 ratified decisions with no verifying test**, but did not perform or report the required
old-versus-incoming decision audit. These conclusions come from the per-branch diffs and worktree
statuses in the evidence ledger below, not from thread summaries.

## Review evidence ledger

The following read-only commands were run on 2026-08-27; labels are cited throughout this report.

- **R1 — branch/worktree authority:** `git worktree list --porcelain`; `git branch --all --no-color`;
  `git log --oneline --decorate -20 --all`. Only `thread/snip-provenance`,
  `thread/phase1-accept`, and `audit/regen-scope` exist among the requested thread branches.
- **R2 — current shared worktree:** `git status --short --branch`; `git diff HEAD --stat`;
  `git diff HEAD --name-status`; `git ls-files --others --exclude-standard`. The tracked diff is
  11 files, 641 insertions, and 1,135 deletions, with additional untracked status/evidence/script/S2
  files.
- **R3 — T2a branch:** `git diff --stat 3ecc4818..thread/snip-provenance` and
  `git diff --name-status 3ecc4818..thread/snip-provenance` report one commit (`794adf46`), nine
  files under `src/data_pipeline/object_extraction/snip_processing/**`, 851 insertions, and 17
  deletions.
- **R4 — T3 branch:** `git status --short --branch --ignored` and
  `find . -type f -newermt '2026-08-24 22:00:00' ! -path './.git/*'` in the T3 worktree returned no
  changed, untracked, ignored, or newly written artifact; `git diff --stat
  3ecc4818..thread/phase1-accept` is empty.
- **R5 — A1 branch:** `git status --short --branch`; `git diff --stat`; and
  `git ls-files --others --exclude-standard` show an unchanged branch at `3ecc4818` plus only
  `docs/refactors/core-model/reports/AUDIT_regeneration_scope.md` as untracked.
- **R6 — decision markers:** `grep -R -n -E
  'pytest\.mark\.decision|@pytest\.mark\.decision' tests --include='*.py'` returned no match.
  Counting incoming `DECISIONS.md` table rows gives 23; counting the git version at
  `HEAD:docs/refactors/core-model/DECISIONS.md` gives 19.
- **R7 — T2a tests:** in the T2a worktree,
  `/net/trapnell/vol1/home/nlammers/micromamba/envs/points-ml/bin/python -m pytest
  tests/data_pipeline/object_extraction/snip_processing/test_run_snip_processing.py
  tests/data_pipeline/object_extraction/snip_processing/test_snip_frame_shape.py
  tests/data_pipeline/object_extraction/segmentation/physical_embryo_registry/test_snip_identity_contract.py
  -q -p no:cacheprovider` passed 37/37 in 39.49 s. The requested `morphseq-env` could not collect the
  suite because `skimage` is absent; that failed collection is not counted as a T2a test failure.
- **R8 — T2a legacy-read check:** T2a's `read_snip_inventory` read the explicit 97-row
  `20250612_30hpf_ctrl_atf6` old inventory and returned both provenance columns as absent with no
  sidecar; the command used the explicit path and did not glob the output tree.
- **R9 — S2 independent census:** an in-memory pandas audit over the first-occurrence ordered 148 IDs
  in `reports/recon_tables/availability_schema.csv`, with artifact paths constructed from each ID,
  reproduced 133 inventories/699,505 rows, 109 stage artifacts/532,235 rows, 531,337 finite stages,
  statuses 511,118 `predicted` + 20,219 `unavailable` + 898 `missing_start_age_hpf`, one model version,
  83,476 legacy rows, 18 common experiments, and zero shared `snip_id` or `embryo_id`.
- **R10 — S2 fingerprints:** `sha256sum` gives
  `64f5f36ca0d76e971364ab71fd45c56d87112884203734d89550b1937e0f9409` for
  `availability_schema.csv`,
  `a8bdace2f8d7a40d57ee6f37ca4c5902ca113d90f9b6c15b783d76d7f1332fd3` for
  `staging_by_experiment.csv`, and
  `4fa7473b0d70255fd796a8b0dd3d23023c1f60c15847e06f65a4e3312cc608f6` for the legacy age key.
- **R11 — A1 timing source:** the historical `20230613` Snakemake log begins
  `[Tue Jul 28 11:16:13 2026]`, ends `[Tue Jul 28 14:15:16 2026]`, and contains 2,638 `Finished job`
  records. Its filesystem mtime is 2026-07-28, so it is a real prior run, not a plate timed by A1.
- **R12 — SeaHub scale:** reading the 92 explicit `frame_inventory_csv` paths in the production
  SeaHub experiment manifest reproduced 8,064 rows, zero inventories with a
  `source_micrometers_per_pixel` column, zero `image_micrometers_per_pixel` values equal to 7.8, and
  range 3.6924676700045973–9.0 with median 7.092735014403527.
- **R13 — environment:**
  `/net/trapnell/vol1/home/nlammers/micromamba/envs/morphseq-env/bin/python -c 'import pyarrow'`
  raises `ModuleNotFoundError` on 2026-08-27.
- **R14 — recon feasibility:** `reports/recon_tables/metric_group_split_feasibility.csv` contains 93
  genotype rows, 22 with `legal_positive_pairs_all_splits=False` (direct CSV count).

## Per-thread disposition

| Thread | Result | What is actually present | What was skipped or silently dropped | Fence result |
|---|---|---|---|---|
| **T1 — status system** | **Partial, not landable** | The shared `core-model-refactor` worktree contains a staged root `DECISIONS.md`, staged root `AGENTS.md`, staged `PLAN.md` and duplicate `THREAD_BRIEFS.md`, an unstaged marker registration, untracked `scripts/status.py` and `STATUS.md`, and untracked evidence copies (R2). The generated status prominently says 23 decisions, 0 with tests, 0 passing, and lists all 23 as unverified (`STATUS.md:59-93`). | No `thread/status-system` branch exists (R1). The canonical `docs/refactors/core-model/{AGENTS,DECISIONS}.md` files were not updated; the copies were written at repository root instead (R2). The attempted moves are deletions plus untracked copies, not committed renames (R2). The status run timed out pytest at 6.02 s and reported zero test results (`STATUS.md:50-57`). No existing test received a decision marker (R6). There is no thread summary/layout inventory, no sourced report of claims that could not be migrated, and no `PROJECT_STATUS.md` contradiction report (absence verified by R2 and `find docs -name PROJECT_STATUS.md`). | **Violation.** Root `AGENTS.md` and root `DECISIONS.md` are outside the `docs/**`, `scripts/status.py`, and conftest-marker fence (`plans/THREAD_BRIEFS.md:13-20`; R2). The marker-only conftest edit is in fence. The `.DS_Store` edit is in-path but needless merge noise (R2). |
| **T2a — snip provenance** | **Partial** | Commit `794adf46` adds a sidecar builder/merger, compatibility reader, required-on-write validation, git/software/rendering metadata, and writer integration in both snip-processing paths (`provenance.py:25-144,214-331`; `inventory_contract.py:30-170`; R3). The real old 97-row inventory read succeeds and reports both fields absent (R8). Existing related tests pass 37/37 (R7). | No new test was added for the sidecar, schema-era detection, old-read compatibility, or merge behavior; `git grep` finds no test reference to `read_snip_inventory` or `SNIP_RENDERING_CONTRACT_VERSION` (R3 plus command). No actual new single-plate output or complete sidecar artifact was recorded, so the brief's done criterion is not met (`plans/THREAD_BRIEFS.md:116-120`). The per-row scale columns were already present in the dispatch base and its tests, so they are not a T2a diff accomplishment (`tests/data_pipeline/object_extraction/snip_processing/test_run_snip_processing.py:313-314`; R3). | **Clean.** All nine changed files are under the exact owned subtree (R3). |
| **T3 — metric unblock/harden/accept** | **Failed** | Nothing. The branch remains exactly at dispatch base and the worktree has no output artifact, including ignored output (R4). | Placeholder mapping/matrix, named configs, decode handling, legal-positive preflight, split tolerance, and all acceptance output were skipped (R4; required work at `plans/THREAD_BRIEFS.md:130-176`). **Acceptance score: 0/8.** The exact step accounting is below. | No changed file exists, so there is no path violation; the thread itself did not execute (R4). |
| **S1 — QC / `sa_outlier_flag`** | **Failed** | No `study/qc-sa-outlier` branch, report, or table exists (R1; `find docs/refactors/core-model/reports -name '*sa_outlier*'`). | Every requested analysis is unresolved: genotype removal rates, control-versus-perturbation gap, isolated `sa_outlier_flag` count, control flags, length adjustment, and policy/cohort size (`plans/THREAD_BRIEFS.md:180-210`). There is no evidence the required pose-confound note was read. That note explicitly says the flag confounds yolk-only mask failures with healthy dorsal/thin-pose embryos and that `k_lower=0.9` deliberately accepts collateral loss (`surface_area_qc_pose_confound.md:8-23,49-83`). Therefore the phenotype-filter hypothesis is **unresolved**, not confirmed. | No branch exists, so a branch stat/fence audit is impossible (R1). No attributable file exists outside the fence. |
| **S2 — stage lineage** | **Partial, useful study** | One untracked report exists on the shared dirty `core-model-refactor` worktree, not on `study/stage-lineage` (`STUDY_stage_lineage.md:3-7`; R1-R2). Its central facts reproduce independently: new stage is deterministic start-age/time/temperature arithmetic, legacy stage uses morphology latents plus MLP/linear calibration, the only new version is `kimmel1995_temp_rate_v1`, coverage/status counts match, and exact old/new ID overlap is zero (`stage_predictions/compute.py:16-75`; `stage_inference.py:15-24`; `src/build/infer_developmental_age.py:46-156`; R9). | Direct correlation/bias/Bland-Altman estimates remain unavailable because paired rows are absent, which the report states rather than hiding (`STUDY_stage_lineage.md:131-148`; R9). No reproducible study script/table was left. The report gives the wrong SHA-256 for `availability_schema.csv` at `:269-272`; R10 gives the actual fingerprint. | The sole attributable file is inside `reports/**`, but branch isolation failed: no S2 branch exists and the file is mixed into T1's shared worktree (R1-R2). |
| **A1 — regeneration scope/timing** | **Partial, useful audit** | One untracked report exists on an otherwise unchanged `audit/regen-scope` branch (R5). It verifies upstream ordinary-corpus readiness, rebuild/verify scope, historical plate timings, GPU-template requirements, scoped disk, and variable SeaHub scale (`AUDIT_regeneration_scope.md:77-104,212-265,284-333`; R11-R12). | A1 did **not** run and time a plate. Its full representative-plate figure is mined from a July 28 historical log (R11). It did not produce the required same-plate server-off/server-on A/B and explicitly says so (`AUDIT_regeneration_scope.md:20-23,174-175`). The 23.17/14.15 task-day totals and 139.01/84.92 h four-task figures are extrapolations, not measurements (`:50-59,177-205`). The current batched legacy-embedding cost for an average plate also remains unmeasured (`:139-141,281-282`). | The only attributable file is inside `reports/**`; path-clean, but uncommitted (R5). |

T2b has no branch or artifact (R1). That is appropriate while T2a lacks its real single-plate sidecar
acceptance; T2b remains gated by `plans/THREAD_BRIEFS.md:275-323`.

## T1 decision audit: the requested answer

**T1 did not do the required audit in a durable artifact.** The only output says the ledgers diverge
and selects the root incoming file (`STATUS.md:59-67`). The script cannot parse the git ledger's
section name (`## 3. Decisions — approved`) because it only recognizes `## Ratified`; it therefore
falls through to the incoming file rather than comparing the two
(`scripts/status.py:368-416`; `docs/refactors/core-model/DECISIONS.md:44-66`). No output names a
dropped decision.

An independent `diff -u <(git show HEAD:docs/refactors/core-model/DECISIONS.md) DECISIONS.md` found:

- No numbered D1–D19 decision vanished silently: D3 and D14 were moved to an explicit superseded
  section; the other numbered decisions remain (`DECISIONS.md:20-54`; git ledger `:48-66`).
- Three old open decision items are absent from the incoming ledger as decision items: mask-jitter
  design, the phase-one experiment list, and checkpoint compatibility (git ledger `:79-88`; incoming
  ledger ends at `DECISIONS.md:69`). The phase-one list and compatibility work appear in PLAN, but
  that is not a decision-ledger carryover (`PLAN.md:66,73-76`).
- The old augmentation commitments/discuss-first entries were not carried into the incoming ledger
  (`docs/refactors/core-model/DECISIONS.md:68-70`; incoming `DECISIONS.md:1-69`).
- D18 changed from `[CLAUDE, pending]` to an unqualified ratified decision without an inline
  ratification source (git ledger `:65`; incoming `DECISIONS.md:37`). This is a status escalation,
  not a mere restructure.

**Unverified-decision count: 23 of 23 on the incoming ledger.** There are zero decision markers in
the tests (R6). The generated status reached the same count but did not run the tests to completion,
so its “0 passing” is a timeout result; the “0 with tests” finding is independently established by
the static marker scan (`STATUS.md:50-65`; R6).

## T3 acceptance: exact step accounting

| Ordered step | Result | Evidence |
|---|---|---|
| 1. Build manifest and inspect contract/join/cohort/split reports | **Not run** | No branch change or output artifact (R4). |
| 2. Open selected images and masks | **Not run** | No output artifact (R4). |
| 3. Iterate train/eval/test loaders at real worker count | **Not run** | No output artifact (R4). |
| 4. One finite basic train/validation step through Lightning | **Not run** | No output artifact (R4). |
| 5. One finite metric step with placeholder mapping | **Not run** | Placeholder files do not exist and branch diff is empty (R4). |
| 6. Tiny `Trainer.fit` with checkpoint, `arch_spec.json`, provenance, and W&B artifact | **Not run** | No output artifact (R4). |
| 7. Reload and reproduce source hashes plus exact `snip_id` set | **Not run** | No checkpoint/reload artifact (R4). |
| 8. DDP/multiworker/GPU repeat and cluster-permission check | **Not run** | No output artifact (R4). |

The cohort-wide legal-positive preflight did not fire; **the number of failing anchors is unknown
because it was never computed** (R4). That silence cannot be interpreted as zero. Existing recon
already shows 22 of 93 genotype groups lacking legal-positive pairs in at least one candidate split
(R14), although only T3's intended placeholder mapping could have produced the requested anchor-level
count. The size-aware split tolerance also did not exist or run, so it could not fire on the small
single-experiment cohort (R4).

## Cross-thread contradictions and corrections

1. **Stage rebuild scope is overstated by A1.** A1 groups stage prediction among products to rebuild
   because they are downstream (`AUDIT_regeneration_scope.md:94-98`). S2 and the producer code show
   that stage values read no snip pixels and are deterministic from plate metadata plus frame time
   (`STUDY_stage_lineage.md:41-66`; `stage_predictions/compute.py:40-75`). Re-emission may still be
   prudent for coherent provenance, but changed rendering does not itself invalidate stage values.
2. **SeaHub is not “all 7.8” and should not be assumed to require a blind full rerun.** PLAN says
   “SeaHub fully” and lists a 7.8 placeholder (`PLAN.md:64,84`). A1's explicit production manifest
   shows no `source_micrometers_per_pixel` column, but all 8,064 stored
   `image_micrometers_per_pixel` values differ from 7.8 and 92 analysis-ready shards exist
   (`AUDIT_regeneration_scope.md:284-333`; R12). Absolute calibration remains placeholder-status;
   numeric uniformity does not.
3. **The environment ledger is stale.** Binding `AGENTS.md` says PyArrow 25.0.1 is installed
   (`AGENTS.md:90-91`), while T1 status and A1 both report it absent
   (`STATUS.md:95-110`; `AUDIT_regeneration_scope.md:345-348`), and R13 confirms absence on
   2026-08-27. Preflight must trust the execution environment, not the prose.
4. **The canonical decision ledger conflicts with binding rules.** It says optical covariates are
   present and should be carried (`docs/refactors/core-model/DECISIONS.md:25-26,50,61`), while binding
   `AGENTS.md` says they do not exist in pipeline schemas (`AGENTS.md:49-51`). A1's SeaHub
   `image_micrometers_per_pixel` is pipeline evidence for a differently named field, not permission to
   populate the four reserved core fields (`AUDIT_regeneration_scope.md:341-344`; R12). The incoming
   T1 ledger corrects this but was written to the wrong path (`DECISIONS.md:49-54`; R2).
5. **The S2 report has a broken evidence fingerprint.** Its corpus numbers reproduce, but the stated
   `availability_schema.csv` hash is wrong (`STUDY_stage_lineage.md:269-272`; R9-R10). The incorrect
   string combines the real prefix with unrelated bytes and should not be used as an authority key.
6. **T1 records decisions as if T3 hardenings existed.** Incoming D11 includes asserted split
   tolerance/empty-split failure and D25 specifies decode recovery (`DECISIONS.md:31,44`), while T3's
   branch is unchanged and PLAN correctly lists both as open (`PLAN.md:80-81`; R4). These are decisions,
   not landed behavior.

## PLAN.md audit and proposed issue-register update

The assertion that the corpus is uniformly regressed remains **inference, not verification**. PLAN
already labels the saturation-distribution argument as inference and names the missing decisive
checks: fraction-at-250 and file mtimes (`PLAN.md:46-50`). T2a only adds provenance for future writes,
S2 is unrelated, and A1 verifies upstream availability/scope rather than rendering era (R3,
`AUDIT_regeneration_scope.md:77-104`). No overnight thread supplied the missing mtime or ≥250 census.

The following is the recommended PLAN issue-register delta; priorities are recommendations based on
the verified blockers above.

| Priority | Proposed register change | Reason/evidence |
|---|---|---|
| **P0** | Keep T2a open as “implementation branch exists; merge blocked on tests plus one real plate producing a complete sidecar.” | Commit exists, old-read passes, real write acceptance absent (R3, R7-R8). |
| **P0** | Keep T2b gated; do not bulk-regenerate before its declared image/model/provenance gates. | T2b absent and T2a not accepted (R1, `plans/THREAD_BRIEFS.md:275-323`). |
| **P0** | Reset T3 status to “not started”: all four hardenings, named presets, and 8-step acceptance remain. | T3 is 0/8 with an empty diff (R4). |
| **P1** | Add “stage-axis semantic migration”: explicit stage method/version in provenance, legacy-to-pipeline crosswalk if available, and `time_window` retune/validation. | New and legacy stages are different estimands and have zero exact-ID overlap (`STUDY_stage_lineage.md:9-35`; R9). |
| **P1** | Keep QC policy open and add the documented pose confound to O2; do not describe S1 as running or phenotype filtering as confirmed. | S1 absent; pose note documents known false exclusion of healthy dorsal/thin animals (`surface_area_qc_pose_confound.md:8-23,49-83`). |
| **P1** | Add a same-plate, current-DAG server-off/server-on timing gate and current batched-embedding timing. Label all current full-corpus time totals extrapolated. | A1 did not run the A/B and says the embedding cost is unmeasured (`AUDIT_regeneration_scope.md:174-205,281-282`; R11). |
| **P1** | Replace SeaHub “full rerun / all 7.8” with “verify/reconcile the 20260804 generation, calibration status, verifier environment, and SAM2 sentinel before choosing rerun scope.” | Stored scale and output state contradict PLAN (`AUDIT_regeneration_scope.md:284-333`; R12). |
| **P1** | Add packaging/environment preflight: clean `data_pipeline` import, Parquet engine, GPU allocation, and write permissions in the exact launch environment. | A1 records the packaging/GPU risks and R13 confirms missing PyArrow (`AUDIT_regeneration_scope.md:212-235,335-354`; R13). |
| **P2** | Keep “uniformly regressed corpus” explicitly inferential until the ordered mtime and fraction-≥250 checks are run. | PLAN itself names those missing checks; no overnight evidence closes them (`PLAN.md:46-50`; R3-R5, R9). |
| **P2** | Repair the status system on a real branch: canonical paths, semantic ledger diff, marker backfill, complete test result, and no root-file overwrite. | T1 is dirty, out of fence, and reports zero tests after timeout (R2, R6; `STATUS.md:50-67`). |

## Recommended next dispatch

1. **Recover T1 into `thread/status-system` before any merge.** Preserve the useful script/PLAN work,
   move edits to the canonical document paths, remove the root-file overwrite from the slice, perform
   the semantic ledger audit above, backfill markers on existing tests, and make the status test result
   complete rather than timeout to zero. Reason: the current mixed worktree is already a conflict
   source and cannot be reviewed as a branch (R1-R2, R6).
2. **Give T2a a narrow acceptance follow-up at `794adf46`.** Add focused tests for sidecar completeness,
   old-read status, parallel merge, and schema-era detection; then run one explicit real plate and
   retain its inventory/sidecar validation output. Reason: provenance is the deadline before any
   expensive rerender, and current code has no evidence for the real-write half (R3, R7-R8).
3. **Redispatch T3 as two checkpoints, not one overnight success claim:** first land the fixture,
   presets, decode handling, legal-positive preflight, and split tolerance with tests; then run the
   eight acceptance steps with an append-only evidence log and exact-ID reload check. Reason: the
   first attempt did literally nothing, and recon already predicts pair failures (R4, R14).
4. **Redispatch S1 with the pose note as a required first citation.** Require genotype/flag tables,
   length-adjusted effect size, and an explicit comparison of phenotype versus pose explanations.
   Reason: O2 cannot be decided from the existing aggregate failure rate and the assigned study is
   absent (`plans/THREAD_BRIEFS.md:180-210`; pose note `:8-23,49-83`).
5. **Commit S2 after correcting its hash, then open a separate stage-policy task.** Do not rerun the
   lineage study; its central measurements reproduce. The next work is an authoritative crosswalk or,
   if none exists, a new-axis pairing/tuning study (R9-R10).
6. **Run A1's missing same-plate A/B only after T2a's accepted writer is selected.** Capture current
   batched embedding time and exact launch resources in the same exercise. Reason: existing historical
   measurements are useful planning inputs, but the served full-plate totals are extrapolations
   (`AUDIT_regeneration_scope.md:174-205`; R11).

## Deliberately not claimed

- No anchor-level legal-positive failure count exists; 22/93 is a group-level feasibility warning,
  not a substitute (R4, R14).
- No current evidence proves that every stored ordinary-corpus snip is regressed; lack of provenance
  justifies caution, not uniform-era certainty (`PLAN.md:43-50`).
- No current evidence establishes that `sa_outlier_flag` is primarily phenotype-driven; the assigned
  study is absent and the standing note establishes a pose/segmentation confound
  (`surface_area_qc_pose_confound.md:8-23`; R1).
- No full-corpus completion date is measured. A1's task-day and four-task totals are extrapolations
  without queue, contention, retry, or same-plate served validation
  (`AUDIT_regeneration_scope.md:50-59,177-205`).
