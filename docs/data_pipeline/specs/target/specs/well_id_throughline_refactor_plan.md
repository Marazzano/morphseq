# well_id_throughline_refactor_plan — moved, deliberately

The real document lives at:

**`docs/refactors/streamline-snakemake/target/specs/well_id_throughline_refactor_plan.md`**

It was **excluded from the 2026-08-24 docs consolidation on purpose.** The document
declares its own path a stable anchor that "must never move," and it is symlinked
into a collaborator's tree at
`/net/trapnell/vol1/home/mdcolon/proj/morphseq/docs/refactors/streamline-snakemake/`.
Moving it would break that symlink from outside this repo.

Sibling docs in this directory cite it by bare filename; follow the path above.

Do not delete this stub or relocate the anchor without coordinating with the
owner of the mdcolon working copy.
